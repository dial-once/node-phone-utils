machine:
  node:
    version: 5.6.0
  environment:
      ENABLE_HLR_LOOKUPS: false
      HLR_LOOKUPS_USERNAME: testUname
      HLR_LOOKUPS_PASSWORD: testPwd
      SMSAPI_USERNAME: testUname
      SMSAPI_HASHED_PASSWORD: testHashPwd
general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
      - /release\/.*/ # or ignore regexes

test:
  pre:
    - npm install mocha-junit-reporter
    - npm install mocha
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/unit
    - MOCHA_FILE=$CIRCLE_TEST_REPORTS/unit/unit-test-results.xml mocha tests --recursive --timeout=5000 --reporter mocha-junit-reporter